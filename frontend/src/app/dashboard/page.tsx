import React from 'react';

const Page = () => {
    return (
        <div>
            this is the dashboard page. this will contain the organizations that the client is managing.
        </div>
    );
};

export default Page;