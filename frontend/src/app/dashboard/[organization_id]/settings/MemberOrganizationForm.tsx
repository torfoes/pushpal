import React from 'react';
import {Organization} from "@/types";

interface OrganizationFormProps {
    organization: Organization;
}
export default function MemberOrganizationForm({ organization }: OrganizationFormProps) {
    return (
        <div>
            do it here
        </div>
    );
};
